{
  "name": "cocktail-assistant",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tsc-watch": "tsc --noEmit --watch",
    "start": "npm run build && npm run preview -- --open"
  },
  "dependencies": {
    "haunted": "^5.0.0",
    "lit-html": "^3.2.1"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  }
}
